name: secret-scan

# Scaffolded by `sdlc onboard` — do not hand-edit the reusable workflow reference.
# To upgrade to a new platform version, run `sdlc onboard --refresh-secret-scan` (planned).
#
# This workflow calls the platform-owned gitleaks scanner in ai-sdlc.
# SHA pins and scanning logic stay platform-maintained — repos supply nothing.
#
# One-time setup required in this repo (human):
#   Settings → Branches → Branch protection for main →
#   Required status checks → add: "gitleaks secret scan"

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  secret-scan:
    uses: piyushgupta27/ai-sdlc/.github/workflows/secret-scan-reusable.yml@main
    secrets: inherit
